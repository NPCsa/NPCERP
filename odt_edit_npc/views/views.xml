<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="odt_edit_accounting.list">
      <field name="name">odt_edit_accounting list</field>
      <field name="model">odt_edit_accounting.odt_edit_accounting</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="odt_edit_accounting.action_window">
      <field name="name">odt_edit_accounting window</field>
      <field name="res_model">odt_edit_accounting.odt_edit_accounting</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="odt_edit_accounting.action_server">
      <field name="name">odt_edit_accounting server</field>
      <field name="model_id" ref="model_odt_edit_accounting_odt_edit_accounting"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="odt_edit_accounting" id="odt_edit_accounting.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="odt_edit_accounting.menu_1" parent="odt_edit_accounting.menu_root"/>
    <menuitem name="Menu 2" id="odt_edit_accounting.menu_2" parent="odt_edit_accounting.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="odt_edit_accounting.menu_1_list" parent="odt_edit_accounting.menu_1"
              action="odt_edit_accounting.action_window"/>
    <menuitem name="Server to list" id="odt_edit_accounting" parent="odt_edit_accounting.menu_2"
              action="odt_edit_accounting.action_server"/>
    -->
  </data>
</odoo>